# Indox.Sdk (C# / .NET)

Official Indox API v1 .NET SDK. Generated project under `generated/`; helper in `src/IndoxClient.cs`.

```bash
make sdk-gen LANG=dotnet
```

Auth: `Authorization: Bearer ak_…` (`INDOX_API_KEY`, `INDOX_BASE_URL`).

**Publish:** NuGet only with explicit approval.
